// Re-export from the canonical client so both import paths work
// Usage:  import { supabase } from '../api/client'
//    or:  import { supabase } from '../../utils/supabase'
export { supabase } from '../../utils/supabase'